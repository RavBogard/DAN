#!/usr/bin/env node
'use strict';

// Stub: will be fully implemented in Task 2
const { error } = require('./core.cjs');

function handle(cwd, args, raw) {
  error('commit module not yet implemented');
}

module.exports = { handle };
