---
phase: 05-autonomy-and-execution
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .claude/skills/dan-pause/SKILL.md
  - .claude/skills/dan-resume/SKILL.md
autonomous: true
requirements:
  - AUTO-06
  - AUTO-07

must_haves:
  truths:
    - "dan:pause SKILL.md contains execution_flow that saves full pipeline position to STATE.md"
    - "dan:resume SKILL.md contains execution_flow that restores position and invokes the correct next skill"
    - "Pause captures phase, plan, task, stage, and wave in STATE.md"
    - "Resume reads saved position and determines which skill to invoke next"
  artifacts:
    - path: ".claude/skills/dan-pause/SKILL.md"
      provides: "Session pause skill with state serialization"
      contains: "save_state"
    - path: ".claude/skills/dan-resume/SKILL.md"
      provides: "Session resume skill with context restoration"
      contains: "restore_context"
  key_links:
    - from: ".claude/skills/dan-pause/SKILL.md"
      to: "bin/lib/session.cjs"
      via: "CLI call to session save"
      pattern: "dan-tools.cjs.*session.*save"
    - from: ".claude/skills/dan-resume/SKILL.md"
      to: "bin/lib/session.cjs"
      via: "CLI call to session restore"
      pattern: "dan-tools.cjs.*session.*restore"
---

<objective>
Write complete execution_flow sections for dan:pause and dan:resume skills, replacing their stubs with full session management workflows.

Purpose: dan:pause saves the current pipeline position so work can be resumed later without human re-explanation. dan:resume reads saved state and continues from where execution stopped. Together they enable session persistence across context window boundaries.

Output: Two fully-specified skill files with executable workflow instructions.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-autonomy-and-execution/05-RESEARCH.md
@.planning/phases/05-autonomy-and-execution/05-01-SUMMARY.md

<interfaces>
<!-- CLI modules from Plan 01 that skills reference -->

From bin/lib/session.cjs (created in Plan 01):
```
Subcommands:
  session save '{"phase":3,"plan":"03-02","task":2,"stage":"apply","wave":1}'
    -> updates STATE.md: status=paused, stopped_at="Phase 3, plan 03-02, task 2, stage: apply"
    -> writes pipeline_phase, pipeline_plan, pipeline_task, pipeline_stage, pipeline_wave to frontmatter

  session restore
    -> {position: {phase, plan, task, stage, wave}, next_action: {action, skill, args}, decisions: [...], blockers: [...]}

  session next-action
    -> {action: "Resume apply on plan 03-02", skill: "dan:apply", args: "03-02"}
```

From bin/lib/state.cjs:
```
  state read         -> full STATE.md content as JSON
  state get "field"  -> single field value
  state set "field" "value" -> update single field
  state patch '{...}' -> update multiple fields
```

From bin/lib/commit.cjs:
```
  commit "message" --files file1 file2 -> atomic git commit
```

STATE.md frontmatter fields for session (from 05-RESEARCH.md):
```yaml
---
status: paused
stopped_at: "Phase 3, plan 03-02, task 2 of 3, stage: apply"
pipeline_phase: 3
pipeline_plan: "03-02"
pipeline_task: 2
pipeline_stage: apply
pipeline_wave: 1
last_updated: "2026-03-29T16:00:00Z"
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dan:pause skill execution_flow</name>
  <files>.claude/skills/dan-pause/SKILL.md</files>
  <action>
    Rewrite .claude/skills/dan-pause/SKILL.md preserving the existing frontmatter but replacing the stub execution_flow with a complete workflow.

    The execution_flow must contain these steps:

    **Step 1: Determine current position**
    - Read STATE.md: `dan-tools.cjs state read`
    - Identify current phase from "Phase" field (e.g., "3 of 5")
    - Identify current plan from "Plan" field
    - Identify pipeline stage from context (what skill was running: research, plan, apply, unify, verify, bugsweep)
    - If user provides position context (e.g., "pausing mid-apply on task 2"), use that.
    - If no explicit position, infer from STATE.md status and last_activity fields.

    **Step 2: Save session state**
    - Build position JSON: {phase, plan, task, stage, wave}
    - Call: `dan-tools.cjs session save '{position_json}'`
    - This sets status=paused, writes pipeline_* fields, updates stopped_at.

    **Step 3: Record context**
    - If there are any in-progress observations, blockers discovered, or decisions pending, append them to STATE.md Accumulated Context sections via `dan-tools.cjs state set`.
    - Update "Last session" field with current date.
    - Update "Last activity" with a description of what was happening.

    **Step 4: Commit WIP**
    - Commit current state: `dan-tools.cjs commit "wip: pause session at {stopped_at}" --files .planning/STATE.md`

    **Step 5: Confirm to user**
    - Display what was saved: position, status, next action recommendation.
    - Tell user: "Run `/dan:resume` to continue from this point."

    The skill runs in-session (no agent spawning). It is a quick state-save operation.
  </action>
  <verify>
    <automated>grep -q "save_state\|session save\|session.*save" .claude/skills/dan-pause/SKILL.md && grep -q "pipeline_stage\|pipeline stage\|stage" .claude/skills/dan-pause/SKILL.md && grep -q "execution_flow" .claude/skills/dan-pause/SKILL.md && grep -q "commit\|wip" .claude/skills/dan-pause/SKILL.md</automated>
  </verify>
  <done>dan:pause SKILL.md has a complete execution_flow that: determines current pipeline position, saves it via session.cjs CLI, records accumulated context, commits WIP state, and confirms to user with resume instructions.</done>
</task>

<task type="auto">
  <name>Task 2: dan:resume skill execution_flow</name>
  <files>.claude/skills/dan-resume/SKILL.md</files>
  <action>
    Rewrite .claude/skills/dan-resume/SKILL.md preserving the existing frontmatter but replacing the stub execution_flow with a complete workflow.

    The execution_flow must contain these steps:

    **Step 1: Restore session state**
    - Call: `dan-tools.cjs session restore`
    - This returns {position, next_action, decisions, blockers}.
    - If status is not "paused", inform user there is no paused session to resume.

    **Step 2: Verify git state**
    - Check that git log contains the WIP commit from pause.
    - Verify no uncommitted changes that conflict with resume.
    - If conflicts found, warn user and ask how to proceed.

    **Step 3: Load accumulated context**
    - Read STATE.md for decisions (from "Decisions" section) -- these are settled, do not re-litigate.
    - Read blockers/concerns -- surface any that were recorded at pause time.
    - Read recent SUMMARY.md files from completed plans in the current phase (if any).
    - Display a brief context summary so Claude understands where things stand.

    **Step 4: Determine next action**
    - Read next_action from restore result: {action, skill, args}.
    - Map pipeline_stage to the correct skill:
      - "research" -> dan:research {phase}
      - "plan" -> dan:plan {phase}
      - "apply" -> dan:apply {plan}
      - "unify" -> dan:unify {plan}
      - "verify" -> dan:verify {phase}
      - "bugsweep" -> dan:bugsweep {phase}
    - If mid-task in a plan (task > 0), pass task offset context to dan:apply.

    **Step 5: Update state and invoke**
    - Update STATE.md status from "paused" to "in_progress" via `dan-tools.cjs state set "Status" "Milestone in progress"`
    - Clear pipeline_* fields or leave them for tracking.
    - Follow the appropriate skill's execution_flow to continue work.

    The skill runs in-session initially for state restoration, then delegates to the appropriate skill for continued execution.
  </action>
  <verify>
    <automated>grep -q "restore_context\|session restore\|session.*restore" .claude/skills/dan-resume/SKILL.md && grep -q "next_action\|next-action\|next action" .claude/skills/dan-resume/SKILL.md && grep -q "execution_flow" .claude/skills/dan-resume/SKILL.md && grep -q "pipeline_stage\|pipeline stage" .claude/skills/dan-resume/SKILL.md</automated>
  </verify>
  <done>dan:resume SKILL.md has a complete execution_flow that: restores session state via session.cjs CLI, verifies git state, loads accumulated context (decisions, blockers, summaries), determines the correct next skill based on pipeline stage, updates STATE.md status, and invokes the appropriate skill to continue execution.</done>
</task>

</tasks>

<verification>
- `grep -q "session save" .claude/skills/dan-pause/SKILL.md` -- pause uses session save CLI
- `grep -q "session restore" .claude/skills/dan-resume/SKILL.md` -- resume uses session restore CLI
- `grep -q "execution_flow" .claude/skills/dan-pause/SKILL.md` -- pause has real execution_flow
- `grep -q "execution_flow" .claude/skills/dan-resume/SKILL.md` -- resume has real execution_flow
- `grep -q "dan:resume" .claude/skills/dan-pause/SKILL.md` -- pause tells user to run resume
</verification>

<success_criteria>
dan:pause SKILL.md contains a complete workflow for saving pipeline position to STATE.md with WIP commit. dan:resume SKILL.md contains a complete workflow for restoring state and invoking the correct next skill based on pipeline stage. Both reference session.cjs CLI subcommands from Plan 01. Together they enable seamless session persistence across context window boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomy-and-execution/05-03-SUMMARY.md`
</output>
