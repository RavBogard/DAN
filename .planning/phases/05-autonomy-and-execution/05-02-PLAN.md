---
phase: 05-autonomy-and-execution
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .claude/skills/dan-milestone/SKILL.md
  - .claude/skills/dan-status/SKILL.md
autonomous: true
requirements:
  - AUTO-01
  - AUTO-02
  - AUTO-03
  - AUTO-04
  - AUTO-05
  - AUTO-08
  - AUTO-09

must_haves:
  truths:
    - "dan:milestone SKILL.md contains a complete execution_flow that chains research, plan, apply, unify, verify, bugsweep across all phases"
    - "dan:milestone execution_flow includes milestone-level and roadmap-level approval gates"
    - "dan:milestone execution_flow includes wave-based parallel execution with file partitioning validation"
    - "dan:milestone execution_flow includes error recovery with retry/skip/escalate options"
    - "dan:status SKILL.md contains a complete execution_flow that reads state and displays progress with next-action suggestion"
  artifacts:
    - path: ".claude/skills/dan-milestone/SKILL.md"
      provides: "Full orchestration skill for autonomous milestone execution"
      contains: "chain_phases"
    - path: ".claude/skills/dan-status/SKILL.md"
      provides: "Status display skill with progress and next-action"
      contains: "next_action"
  key_links:
    - from: ".claude/skills/dan-milestone/SKILL.md"
      to: "bin/lib/milestone.cjs"
      via: "CLI calls for pipeline-order, validate-wave, record-error, progress"
      pattern: "dan-tools.cjs.*milestone"
    - from: ".claude/skills/dan-milestone/SKILL.md"
      to: ".claude/skills/dan-research/SKILL.md"
      via: "chains dan:research as first pipeline stage"
      pattern: "dan:research"
    - from: ".claude/skills/dan-status/SKILL.md"
      to: "bin/lib/milestone.cjs"
      via: "CLI calls for status and progress"
      pattern: "dan-tools.cjs.*milestone"
    - from: ".claude/skills/dan-status/SKILL.md"
      to: "bin/lib/session.cjs"
      via: "CLI calls for next-action"
      pattern: "dan-tools.cjs.*session.*next-action"
---

<objective>
Write complete execution_flow sections for dan:milestone and dan:status skills, replacing their stubs with full orchestration logic.

Purpose: dan:milestone is the capstone skill that chains the entire DAN pipeline autonomously. dan:status provides visibility into progress and suggests the next action. Both reference CLI modules built in Plan 01.

Output: Two fully-specified skill files with executable workflow instructions.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-autonomy-and-execution/05-RESEARCH.md
@.planning/phases/05-autonomy-and-execution/05-01-SUMMARY.md

<interfaces>
<!-- CLI modules from Plan 01 that skills reference -->

From bin/lib/milestone.cjs (created in Plan 01):
```
Subcommands:
  milestone status              -> {milestone, status, phases_complete, phases_remaining, current_phase, pipeline_stage}
  milestone pipeline-order      -> {order: [phaseNum, ...]}
  milestone validate-wave DIR   -> {valid: bool, conflicts?: [...]}
  milestone record-error N STG REASON -> patches STATE.md
  milestone progress            -> {total_phases, completed_phases, total_plans, completed_plans, percent}
```

From bin/lib/session.cjs (created in Plan 01):
```
Subcommands:
  session save '{"phase":3,"plan":"03-02","task":2,"stage":"apply"}'  -> saves to STATE.md
  session restore               -> {position, next_action, decisions, blockers}
  session next-action           -> {action, skill, args}
```

Existing skill patterns (all skills follow this structure):
- Frontmatter: name, description, argument-hint, disable-model-invocation, agent-skills
- Body: markdown description + <execution_flow> with <step> elements
- Steps use CLI calls: node "$HOME/.claude/dan/bin/dan-tools.cjs" --cwd $PROJECT_DIR <command>
- Steps reference other skills by name: "Follow dan:apply execution_flow"
- Two-level rule: skills orchestrate, agents execute. Skills never spawn from agents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dan:milestone skill execution_flow</name>
  <files>.claude/skills/dan-milestone/SKILL.md</files>
  <action>
    Rewrite .claude/skills/dan-milestone/SKILL.md preserving the existing frontmatter but replacing the stub execution_flow with a complete orchestration workflow.

    The execution_flow must contain these steps (using semantic XML <step> elements):

    **Step 1: Approval gate (AUTO-02, AUTO-03)**
    - If invoked with `--all` flag, this is roadmap-level approval (AUTO-03). Confirm user wants to run ALL milestones.
    - Otherwise, milestone-level approval (AUTO-02). Show milestone scope (phases, estimated plans) via `dan-tools.cjs milestone status`.
    - Ask user to confirm. If declined, stop. After approval, run autonomously with no further human gates.
    - Record approval in STATE.md via `dan-tools.cjs state patch`.

    **Step 2: Pipeline order (AUTO-01)**
    - Get phase execution order: `dan-tools.cjs milestone pipeline-order`
    - Filter to remaining phases only (skip completed ones).

    **Step 3: Phase pipeline loop (AUTO-01)**
    - For each phase in order:
      a. **Research**: Read the phase's research skill execution_flow. Run `dan:research {phase}` if no RESEARCH.md exists yet.
      b. **Plan**: Run `dan:plan {phase}` if no PLAN.md files exist yet.
      c. **Apply with waves (AUTO-04, AUTO-05)**:
         - Get wave grouping: `dan-tools.cjs dependency waves {phaseDir}`
         - Validate partitioning: `dan-tools.cjs milestone validate-wave {phaseDir}`
         - If conflicts detected and parallelization enabled, fall back to sequential for that wave.
         - For each wave (sequential):
           - For each plan in wave: run `dan:apply {plan}` then `dan:unify {plan}`
           - If parallelization enabled and partitioning valid: spawn multiple executor agents in parallel (Task() calls), wait for all to complete, then run dan:unify for each.
           - If sequential: run each plan's apply+unify in order.
      d. **Verify**: Run `dan:verify {phase}`
      e. **Bugsweep**: Run `dan:bugsweep {phase}` if verification has gaps
      f. **Advance**: Update STATE.md via `dan-tools.cjs state patch`, advance phase via `dan-tools.cjs phase complete`

    **Step 4: Error recovery (AUTO-09)**
    - Wrap each pipeline stage in error handling.
    - On failure: `dan-tools.cjs milestone record-error {phase} {stage} {reason}`
    - Offer three options: retry from failed stage, skip phase (warn about downstream deps), escalate to user.
    - Phase-level retry cap of 1. If retry also fails, escalate.

    **Step 5: Completion**
    - When all phases complete: update STATE.md status to "completed", update ROADMAP.md progress.
    - Display final progress via `dan-tools.cjs milestone progress`.

    Keep the existing "What It Does", "Execution Mode", and "CLI Tools" sections. Add the full execution_flow below them.

    IMPORTANT: The skill is a markdown prompt file, NOT executable code. Write it as instructions Claude follows, with CLI commands it runs. Use `<step>`, `<if>`, `<for_each>` semantic XML for structure.
  </action>
  <verify>
    <automated>grep -q "chain_phases\|pipeline_order\|pipeline-order" .claude/skills/dan-milestone/SKILL.md && grep -q "error_recovery\|record-error\|record_error" .claude/skills/dan-milestone/SKILL.md && grep -q "validate-wave\|validate_wave" .claude/skills/dan-milestone/SKILL.md && grep -q "approval\|approve" .claude/skills/dan-milestone/SKILL.md && grep -q "execution_flow" .claude/skills/dan-milestone/SKILL.md</automated>
  </verify>
  <done>dan:milestone SKILL.md has a complete execution_flow with: approval gate (milestone and roadmap level), pipeline ordering via CLI, phase loop with research/plan/apply/unify/verify/bugsweep stages, wave-based parallel execution with partitioning validation, error recovery with retry/skip/escalate, and completion handling. All CLI calls reference dan-tools.cjs milestone and session subcommands.</done>
</task>

<task type="auto">
  <name>Task 2: dan:status skill execution_flow</name>
  <files>.claude/skills/dan-status/SKILL.md</files>
  <action>
    Rewrite .claude/skills/dan-status/SKILL.md preserving the existing frontmatter (especially `disable-model-invocation: false`) but replacing the stub execution_flow with a complete workflow.

    The execution_flow must contain these steps:

    **Step 1: Gather state**
    - Read STATE.md: `dan-tools.cjs state read`
    - Get milestone progress: `dan-tools.cjs milestone progress`
    - Get milestone status: `dan-tools.cjs milestone status`

    **Step 2: Determine next action**
    - Call: `dan-tools.cjs session next-action`
    - This returns {action, skill, args} based on priority logic in session.cjs.

    **Step 3: Display status**
    - Show phase position (e.g., "Phase 3 of 5: Research System")
    - Show progress bar and percentage
    - Show current plan status if any plan is IN_PROGRESS
    - Show pipeline stage if milestone is running
    - Show any blockers or concerns from STATE.md
    - Show recent decisions (last 3) from STATE.md
    - Show suggested next action from Step 2

    **Step 4: Format output**
    - Use a clean markdown format with headers
    - Progress bar uses the format from phase.cjs renderProgressBar
    - Next action is highlighted as a command the user can run

    Keep the skill read-only -- no state modifications. Keep `disable-model-invocation: false` so Claude can auto-invoke this skill.
  </action>
  <verify>
    <automated>grep -q "phase_position\|phase position\|Phase.*of" .claude/skills/dan-status/SKILL.md && grep -q "next_action\|next-action\|next action" .claude/skills/dan-status/SKILL.md && grep -q "execution_flow" .claude/skills/dan-status/SKILL.md && grep -q "milestone progress\|milestone status" .claude/skills/dan-status/SKILL.md</automated>
  </verify>
  <done>dan:status SKILL.md has a complete execution_flow that reads STATE.md and ROADMAP.md, displays phase position with progress bar, shows blockers and recent decisions, and suggests the next action via session next-action CLI. The skill remains read-only and auto-invocable.</done>
</task>

</tasks>

<verification>
- `grep -q "chain_phases\|pipeline_order" .claude/skills/dan-milestone/SKILL.md` -- milestone has pipeline chaining
- `grep -q "error_recovery\|record-error" .claude/skills/dan-milestone/SKILL.md` -- milestone has error recovery
- `grep -q "validate-wave" .claude/skills/dan-milestone/SKILL.md` -- milestone validates wave partitioning
- `grep -q "next_action\|next-action" .claude/skills/dan-status/SKILL.md` -- status suggests next action
- `grep -q "execution_flow" .claude/skills/dan-milestone/SKILL.md` -- milestone has execution_flow (not stub)
- `grep -q "execution_flow" .claude/skills/dan-status/SKILL.md` -- status has execution_flow (not stub)
</verification>

<success_criteria>
dan:milestone SKILL.md contains a complete pipeline orchestration workflow covering all 6 stages (research through bugsweep), approval gates (milestone and roadmap level), wave-based parallel execution with file partitioning, and error recovery. dan:status SKILL.md contains a complete read-only workflow showing progress and next-action suggestions. Both reference CLI modules from Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomy-and-execution/05-02-SUMMARY.md`
</output>
