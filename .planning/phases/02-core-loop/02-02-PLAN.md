---
phase: 02-core-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .claude/skills/dan-plan/SKILL.md
  - .claude/skills/dan-unify/SKILL.md
  - .claude/agents/dan-planner.md
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-07
  - LOOP-08

must_haves:
  truths:
    - "dan:plan skill has a complete execution_flow that orchestrates plan creation with precondition checks"
    - "Orphan plan detection blocks new plan creation when unclosed loops exist"
    - "dan:unify skill has a complete execution_flow that produces SUMMARY.md and updates state"
    - "Every PLAN.md gets a SUMMARY.md -- no orphan plans"
    - "Plans are sized to 2-3 tasks per plan"
  artifacts:
    - path: ".claude/skills/dan-plan/SKILL.md"
      provides: "Full plan creation workflow with orphan detection, lifecycle transitions, and planner agent spawning"
      contains: "check_loop_closure"
    - path: ".claude/skills/dan-unify/SKILL.md"
      provides: "Full loop closure workflow producing SUMMARY.md with plan-vs-actual reconciliation"
      contains: "read_plan_and_commits"
    - path: ".claude/agents/dan-planner.md"
      provides: "Enhanced planner agent with detailed execution flow for producing sized plans"
      contains: "Execution Flow"
  key_links:
    - from: ".claude/skills/dan-plan/SKILL.md"
      to: "bin/lib/lifecycle.cjs"
      via: "references lifecycle validation for DRAFT status setting"
      pattern: "lifecycle.*validate|DRAFT"
    - from: ".claude/skills/dan-plan/SKILL.md"
      to: ".claude/agents/dan-planner.md"
      via: "spawns planner agent to produce plan files"
      pattern: "dan-planner"
    - from: ".claude/skills/dan-unify/SKILL.md"
      to: "bin/lib/lifecycle.cjs"
      via: "transitions plan to COMPLETED state"
      pattern: "COMPLETED|lifecycle"
---

<objective>
Fill in the execution_flow sections for dan:plan and dan:unify skills, and enhance the dan-planner agent prompt with detailed plan generation instructions. The plan skill orchestrates plan creation with orphan detection and lifecycle management. The unify skill closes loops by producing SUMMARY.md files. Together they bookend the loop: plan opens it, unify closes it.

Purpose: These two skills define the start and end of every work loop. Without them, the apply skill (Plan 03) has nothing to execute and no way to close the loop. The orphan detection in dan:plan enforces mandatory loop closure (LOOP-08).

Output: Two fully-specified skill workflows (dan:plan, dan:unify) and an enhanced planner agent prompt.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop/02-RESEARCH.md
</context>

<interfaces>
<!-- From Plan 01 deliverables -->

From bin/lib/lifecycle.cjs (created in 02-01):
```javascript
const PLAN_STATES = { DRAFT: 'DRAFT', APPROVED: 'APPROVED', IN_PROGRESS: 'IN_PROGRESS', COMPLETED: 'COMPLETED', ABANDONED: 'ABANDONED' };
const VALID_TRANSITIONS = { DRAFT: ['APPROVED', 'ABANDONED'], APPROVED: ['IN_PROGRESS', 'ABANDONED'], IN_PROGRESS: ['COMPLETED', 'ABANDONED'], COMPLETED: [], ABANDONED: [] };
function validateTransition(from, to) -> { valid: boolean, reason?: string }
function getNextStates(state) -> string[]
function isTerminal(state) -> boolean
```

From bin/lib/qualify.cjs (created in 02-01):
```javascript
const QUALIFICATION_STATUSES = { PASS: 'PASS', PASS_WITH_CONCERNS: 'PASS_WITH_CONCERNS', NEEDS_REVISION: 'NEEDS_REVISION', FAIL: 'FAIL' };
function parseQualifierOutput(text) -> { status, task, criteria, evidence, issues }
function shouldRetry(status, retryCount, maxRetries) -> { retry: boolean, reason: string }
function classifyFailure(taskSpec, evidence, objective) -> { classification: string, reasoning: string }
```

From bin/templates/plan.md (existing):
- YAML frontmatter with phase, plan, type, wave, depends_on, files_modified, autonomous, requirements, must_haves
- XML task format with type, name, files, behavior, action, verify, done

From bin/templates/summary.md (existing):
- Frontmatter with phase, plan, completed date
- Sections: Objective, What Was Built, Plan vs Actual, Decisions, Deviations, Deferred
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: dan:plan skill execution_flow and dan-planner agent enhancement</name>
  <files>.claude/skills/dan-plan/SKILL.md, .claude/agents/dan-planner.md</files>
  <action>
    Replace the placeholder `<execution_flow>` in `.claude/skills/dan-plan/SKILL.md` with a complete workflow. The skill is the orchestrator (runs in the user's session); it does NOT generate plans itself -- it spawns the dan-planner agent.

    The execution_flow must include these steps (use semantic XML `<step>` elements):

    1. **check_loop_closure** (priority: first):
       - Read STATE.md to get current phase and phase directory
       - List all *-PLAN.md files in the phase directory
       - List all *-SUMMARY.md files in the phase directory
       - For each PLAN without a matching SUMMARY:
         - Parse frontmatter to check status field
         - If status is IN_PROGRESS or APPROVED: BLOCK with message "Previous plan [XX-NN] is [status]. Run /dan:apply to complete or /dan:unify to close the loop."
         - If status is ABANDONED: verify SUMMARY exists (abandoned plans still need summaries)
       - Only proceed if all prior loops are closed (COMPLETED or ABANDONED with SUMMARY)

    2. **determine_phase**:
       - If phase number argument provided, use it
       - Otherwise read STATE.md for current phase
       - Verify phase directory exists under .planning/phases/
       - Read RESEARCH.md if it exists for the phase
       - Determine next plan number (count existing PLANs + 1)

    3. **spawn_planner**:
       - Spawn dan-planner agent with context:
         - Path to RESEARCH.md (or ROADMAP.md phase section if no research)
         - Path to REQUIREMENTS.md
         - Path to PROJECT.md
         - Phase directory path
         - Plan number
         - Paths to any existing SUMMARY.md files in the phase (for context on completed work)
       - The planner produces PLAN.md file(s) with frontmatter status: DRAFT

    4. **present_for_approval**:
       - After planner completes, read the produced PLAN.md
       - Present plan summary to user: objective, task count, files modified, requirements covered
       - Wait for user approval
       - On approval: update plan frontmatter status from DRAFT to APPROVED using dan-tools.cjs or direct edit
       - On rejection: note feedback, optionally re-spawn planner with revision instructions

    5. **update_state**:
       - Update STATE.md: "Plan: XX-NN created and approved"
       - Commit plan file: `dan-tools.cjs commit "plan(XX-NN): [brief objective]" --files [plan-path]`

    Also enhance `.claude/agents/dan-planner.md`:
    - Add a detailed "Execution Flow" section to the agent's role that specifies:
      - Read all provided context files (research, requirements, project, existing summaries)
      - Identify uncovered requirements for this phase
      - Decompose into 2-3 tasks per plan (LOOP-02 sizing rule)
      - Each task must have: type, name, files, action (specific), verify (automated command), done (measurable)
      - Set frontmatter: phase, plan, type, wave, depends_on, files_modified, autonomous, requirements, status: DRAFT
      - Include must_haves derived goal-backward from the plan objective
      - Write plan file to phase directory
    - Add plan sizing rules: 2-3 tasks max, each task 15-60 min Claude execution, split if >5 files modified
    - Add the plan template reference: bin/templates/plan.md
  </action>
  <verify>
    <automated>grep -c "check_loop_closure\|determine_phase\|spawn_planner\|present_for_approval\|update_state" .claude/skills/dan-plan/SKILL.md | grep -v "^0$"</automated>
  </verify>
  <done>
    - dan:plan SKILL.md has complete execution_flow with 5 named steps
    - check_loop_closure step validates no orphan plans exist before creating new ones
    - spawn_planner step passes all necessary context to dan-planner agent
    - present_for_approval step handles DRAFT->APPROVED transition
    - dan-planner.md has detailed plan generation instructions with sizing rules (2-3 tasks)
    - Plan template reference included in planner agent
  </done>
</task>

<task type="auto">
  <name>Task 2: dan:unify skill execution_flow</name>
  <files>.claude/skills/dan-unify/SKILL.md</files>
  <action>
    Replace the placeholder `<execution_flow>` in `.claude/skills/dan-unify/SKILL.md` with a complete workflow. The unify skill runs in-session (no agent spawning needed -- it reads files and produces SUMMARY.md).

    The execution_flow must include these steps:

    1. **identify_plan**:
       - If plan path argument provided, use it
       - Otherwise: read STATE.md for current phase and plan position
       - Find the most recent PLAN.md without a matching SUMMARY.md
       - Parse plan frontmatter -- verify status is COMPLETED or IN_PROGRESS (warn if IN_PROGRESS)
       - If no unclosed plans found: "All plans have summaries. Nothing to unify."

    2. **read_plan_and_commits**:
       - Read the PLAN.md file: extract objective, tasks, success criteria, requirements
       - Read git log for commits matching the plan's phase-plan prefix (e.g., `git log --oneline --grep="(02-01)"`)
       - For each task in the plan: determine if it was completed, skipped, or modified
       - Read the plan's files_modified list and check current state of those files

    3. **gather_qualification_results**:
       - Check if qualifier was used (look for qualification result patterns in STATE.md decisions or concerns)
       - Collect any PASS_WITH_CONCERNS notes
       - Note any deviations (auto-fixed bugs, added missing functionality, etc.)

    4. **produce_summary**:
       - Create SUMMARY.md using the summary template structure (bin/templates/summary.md)
       - Fill in:
         - Frontmatter: phase, plan, completed date
         - One-liner: substantive description of what was built
         - Objective: from the plan
         - What Was Built: list of actual deliverables with file paths
         - Plan vs Actual: table comparing planned tasks/files/duration vs actual
         - Decisions Made: any decisions logged during execution
         - Deviations from Plan: differences between planned and actual
         - Deferred Items: anything planned but not completed
       - Write to `.planning/phases/{phase}/{phase}-{plan}-SUMMARY.md`

    5. **update_state_and_roadmap**:
       - Transition plan frontmatter status to COMPLETED (using lifecycle.cjs validation rules)
       - Update STATE.md:
         - Advance plan counter
         - Add any new decisions to the Decisions section
         - Update last activity
       - Update ROADMAP.md: mark the plan as [x] complete in the plans list
       - Commit: `dan-tools.cjs commit "unify(XX-NN): close loop with summary" --files [summary-path] [state-path] [roadmap-path]`

    6. **offer_next**:
       - If more plans in the phase: "Next: Run /dan:apply on plan XX-NN+1"
       - If all plans in phase complete: "Phase complete. Run /dan:verify to validate phase deliverables."
       - Always remind: the loop is now closed for plan XX-NN
  </action>
  <verify>
    <automated>grep -c "identify_plan\|read_plan_and_commits\|gather_qualification\|produce_summary\|update_state_and_roadmap\|offer_next" .claude/skills/dan-unify/SKILL.md | grep -v "^0$"</automated>
  </verify>
  <done>
    - dan:unify SKILL.md has complete execution_flow with 6 named steps
    - identify_plan step finds unclosed plans and validates status
    - read_plan_and_commits step gathers execution evidence from git log
    - produce_summary step creates SUMMARY.md with plan-vs-actual comparison
    - update_state_and_roadmap step transitions to COMPLETED and updates progress
    - offer_next step suggests the appropriate next action
    - Mandatory loop closure enforced: every plan gets a summary
  </done>
</task>

</tasks>

<verification>
- dan:plan SKILL.md contains complete execution_flow with orphan detection and lifecycle management
- dan:unify SKILL.md contains complete execution_flow with summary production and state updates
- dan-planner.md has detailed plan generation instructions with 2-3 task sizing rule
- All workflow steps reference dan-tools.cjs CLI commands (not inline regex/parsing)
- Orphan detection logic correctly checks for PLAN files without matching SUMMARY files
- `node --test bin/tests/` still passes (no JS code broken)
</verification>

<success_criteria>
- dan:plan has a 5-step execution_flow covering: orphan check, phase detection, planner spawning, approval gate, state update
- dan:unify has a 6-step execution_flow covering: plan identification, evidence gathering, summary production, state/roadmap update, next action suggestion
- dan-planner agent has specific plan generation instructions including sizing rules (2-3 tasks, 15-60 min each)
- Orphan plan detection blocks new plans when unclosed loops exist (LOOP-08)
- Plans are sized to 2-3 tasks (LOOP-02 enforced in planner instructions)
- SUMMARY.md follows the existing template structure (LOOP-07)
- All references to state operations use dan-tools.cjs CLI (not raw regex)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop/02-02-SUMMARY.md`
</output>
