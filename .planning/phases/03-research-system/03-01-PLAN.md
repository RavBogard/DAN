---
phase: 03-research-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/research.cjs
  - bin/dan-tools.cjs
  - bin/tests/test-research.cjs
autonomous: true
requirements:
  - RSRCH-02
  - RSRCH-05
  - RSRCH-06

must_haves:
  truths:
    - "research init creates state.json with correct schema for project or phase target"
    - "record-pass appends pass data with gaps and confidence to state"
    - "check-convergence returns should_continue=false when hard cap reached, gaps=0, all HIGH, or diminishing returns"
    - "check-convergence returns should_continue=true when gaps remain and progress is being made"
  artifacts:
    - path: "bin/lib/research.cjs"
      provides: "Research state tracking CLI module"
      exports: ["handle", "initResearch", "recordPass", "checkConvergence", "getStatus"]
    - path: "bin/tests/test-research.cjs"
      provides: "Unit tests for research module"
      min_lines: 80
    - path: "bin/dan-tools.cjs"
      provides: "Router with research command"
      contains: "case 'research'"
  key_links:
    - from: "bin/dan-tools.cjs"
      to: "bin/lib/research.cjs"
      via: "require and case dispatch"
      pattern: "require.*research\\.cjs.*\\.handle"
    - from: "bin/lib/research.cjs"
      to: "bin/lib/core.cjs"
      via: "atomicWriteFileSync for state persistence"
      pattern: "atomicWriteFileSync"
---

<objective>
Build the research state tracking CLI module that powers the recursive research loop.

Purpose: The dan:research skill orchestrator needs CLI commands to initialize research state, record pass results, and check convergence. This module is the backbone of the multi-pass research loop -- it tracks pass number, gaps, confidence per dimension, and determines when research should stop.

Output: bin/lib/research.cjs module with handle() export, tests, and router wiring in dan-tools.cjs.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-research-system/03-RESEARCH.md

<interfaces>
<!-- Existing core.cjs exports used by all modules -->
From bin/lib/core.cjs:
```javascript
module.exports = { output, error, atomicWriteFileSync, execGit, toPosixPath, loadConfig };
```

From bin/lib/state.cjs (pattern reference for handle function):
```javascript
module.exports = { handle, extractField, setField };
// handle(cwd, subArgs, raw) dispatches subcommands: get, set, read, write
```

From bin/dan-tools.cjs (router pattern):
```javascript
case 'qualify':
  require('./lib/qualify.cjs').handle(cwd, subArgs, raw);
  break;
// Add 'research' case following this pattern
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Research CLI module with tests</name>
  <files>bin/lib/research.cjs, bin/tests/test-research.cjs</files>
  <behavior>
    - initResearch("project", 4) creates state.json at .planning/research/state.json with target="project", max_passes=4, current_pass=0, passes=[], converged=false
    - initResearch("3", 4) creates state.json at .planning/phases/03-*/research/state.json
    - recordPass records pass data: pass number, completed timestamp, researchers list, gaps_found count, gaps array, confidence object
    - recordPass updates current_pass to the recorded pass number
    - checkConvergence returns should_continue=false, reason contains "hard cap" when current_pass >= max_passes
    - checkConvergence returns should_continue=false, reason contains "no gaps" when last pass has gaps_found=0
    - checkConvergence returns should_continue=false, reason contains "HIGH" when all confidence dimensions are "HIGH"
    - checkConvergence returns should_continue=false, reason contains "diminishing" when gaps_found >= previous pass gaps_found
    - checkConvergence returns should_continue=true when gaps remain and are decreasing
    - getStatus returns full state object as JSON
    - handle dispatches subcommands: init, record-pass, check-convergence, status
  </behavior>
  <action>
    Write tests FIRST in bin/tests/test-research.cjs following the pattern in test-core.cjs (node:test, node:assert/strict, tmpDir setup/teardown). Test each convergence condition independently. Use a temp directory to create fake .planning/research/ and .planning/phases/03-test/ structures.

    Then implement bin/lib/research.cjs following the code example in 03-RESEARCH.md "Code Examples" section. Key implementation details:

    - getStatePath(cwd, target): "project" -> .planning/research/state.json; numeric -> find phase dir under .planning/phases/ matching padded number prefix
    - initResearch(cwd, target, maxPasses): create dir recursively, write initial state via atomicWriteFileSync
    - recordPass(cwd, target, passNum, gaps, confidence): read state, push pass entry, update current_pass, write back
    - checkConvergence(cwd, target): implement 4 checks in order: (1) hard cap, (2) no gaps, (3) all HIGH, (4) diminishing returns
    - getStatus(cwd, target): read and return state object
    - handle(cwd, subArgs, raw): dispatch subcommands with proper arg parsing (--pass, --gaps, --confidence, --max-passes flags)

    Use atomicWriteFileSync from core.cjs for all writes. Use output() for JSON results. Use error() for failures.
  </action>
  <verify>
    <automated>node --test bin/tests/test-research.cjs</automated>
  </verify>
  <done>All convergence conditions tested and passing. Module exports handle, initResearch, recordPass, checkConvergence, getStatus.</done>
</task>

<task type="auto">
  <name>Task 2: Wire research module into dan-tools.cjs router</name>
  <files>bin/dan-tools.cjs</files>
  <action>
    Add a 'research' case to the switch statement in dan-tools.cjs, following the existing pattern (e.g., the 'qualify' case). The case should require('./lib/research.cjs').handle(cwd, subArgs, raw).

    Update the error message's command list to include 'research' in the available commands string.

    Verify the full test suite still passes after the change.
  </action>
  <verify>
    <automated>node bin/dan-tools.cjs research init project --cwd "C:/Users/dsbog/dan" 2>&1 | grep -q "target" && node --test bin/tests/</automated>
  </verify>
  <done>Running `dan-tools.cjs research init project` produces valid JSON output. Full test suite green.</done>
</task>

</tasks>

<verification>
- `node --test bin/tests/test-research.cjs` passes all convergence and state management tests
- `node --test bin/tests/` full suite stays green (126+ tests)
- `node bin/dan-tools.cjs research init project --cwd .` produces valid JSON
- `node bin/dan-tools.cjs research check-convergence project --cwd .` returns should_continue field
</verification>

<success_criteria>
Research CLI module exists with init, record-pass, check-convergence, and status subcommands. All four convergence conditions (hard cap, zero gaps, all HIGH, diminishing returns) are tested and working. Router wiring allows `dan-tools.cjs research <subcommand>` invocation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-research-system/03-01-SUMMARY.md`
</output>
