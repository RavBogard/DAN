---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .claude/skills/dan-init/SKILL.md
  - .claude/skills/dan-discuss/SKILL.md
  - .claude/skills/dan-research/SKILL.md
  - .claude/skills/dan-plan/SKILL.md
  - .claude/skills/dan-apply/SKILL.md
  - .claude/skills/dan-unify/SKILL.md
  - .claude/skills/dan-verify/SKILL.md
  - .claude/skills/dan-bugsweep/SKILL.md
  - .claude/skills/dan-milestone/SKILL.md
  - .claude/skills/dan-status/SKILL.md
  - .claude/skills/dan-pause/SKILL.md
  - .claude/skills/dan-resume/SKILL.md
  - .claude/skills/dan-workflow/SKILL.md
  - .claude/agents/dan-researcher.md
  - .claude/agents/dan-synthesizer.md
  - .claude/agents/dan-planner.md
  - .claude/agents/dan-executor.md
  - .claude/agents/dan-qualifier.md
  - .claude/agents/dan-verifier.md
  - .claude/agents/dan-auditor.md
autonomous: true
requirements:
  - FOUND-07
  - FOUND-08
  - FOUND-09

must_haves:
  truths:
    - "All 12 skill entry points are discoverable via `/dan:` prefix in Claude Code autocomplete"
    - "Each skill SKILL.md has valid frontmatter with name, description, and disable-model-invocation fields"
    - "All 7 agent definitions exist with appropriate tool restrictions per role"
    - "No agent definition contains instructions to spawn other agents (two-level hierarchy enforced)"
    - "The dan-workflow skill exists and is referenced in agent skills: frontmatter for shared protocol injection"
  artifacts:
    - path: ".claude/skills/dan-plan/SKILL.md"
      provides: "Plan skill entry point"
      contains: "name: dan:plan"
    - path: ".claude/skills/dan-apply/SKILL.md"
      provides: "Apply skill entry point"
      contains: "name: dan:apply"
    - path: ".claude/skills/dan-workflow/SKILL.md"
      provides: "Shared workflow rules injected into agents"
      contains: "loop protocol"
    - path: ".claude/agents/dan-executor.md"
      provides: "Plan execution agent with full tool access"
      contains: "tools:"
    - path: ".claude/agents/dan-qualifier.md"
      provides: "Independent qualification agent with read-only tools"
      contains: "tools: Read, Grep, Glob, Bash"
  key_links:
    - from: ".claude/agents/dan-qualifier.md"
      to: ".claude/skills/dan-workflow/"
      via: "skills: frontmatter referencing dan-workflow"
      pattern: "skills:.*dan-workflow"
    - from: ".claude/skills/dan-apply/SKILL.md"
      to: "bin/dan-tools.cjs"
      via: "CLI tool invocation in workflow instructions"
      pattern: "dan-tools\\.cjs"
    - from: ".claude/agents/dan-executor.md"
      to: ".claude/skills/dan-workflow/"
      via: "skills: frontmatter"
      pattern: "skills:.*dan-workflow"
---

<objective>
Create all 12 DAN skill entry points and 7 agent definitions with correct frontmatter, tool restrictions, and the shared dan-workflow skill for protocol injection.

Purpose: Skills are the user-facing entry points (how users invoke DAN). Agents are the worker definitions (how DAN executes tasks). The two-level hierarchy (skills orchestrate, agents execute) is a hard architectural constraint. Getting this scaffolding right ensures all future phases have correct invocation and execution paths.
Output: 12 skill SKILL.md files, 7 agent .md files, 1 shared workflow skill with loop protocol.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<!-- Depends on Plan 01 for CLI tools path references in skill/agent bodies -->
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skill entry points and shared workflow skill</name>
  <files>.claude/skills/dan-init/SKILL.md, .claude/skills/dan-discuss/SKILL.md, .claude/skills/dan-research/SKILL.md, .claude/skills/dan-plan/SKILL.md, .claude/skills/dan-apply/SKILL.md, .claude/skills/dan-unify/SKILL.md, .claude/skills/dan-verify/SKILL.md, .claude/skills/dan-bugsweep/SKILL.md, .claude/skills/dan-milestone/SKILL.md, .claude/skills/dan-status/SKILL.md, .claude/skills/dan-pause/SKILL.md, .claude/skills/dan-resume/SKILL.md, .claude/skills/dan-workflow/SKILL.md</files>
  <action>
    Create 12 skill entry point SKILL.md files in `.claude/skills/dan-*/SKILL.md`. Each skill needs:

    **Frontmatter (all skills):**
    - `name: dan:<action>` (e.g., `dan:plan`, `dan:apply`)
    - `description:` one-line purpose
    - `argument-hint:` show accepted args (e.g., `"[phase-number]"`, `"[plan-path]"`)
    - `disable-model-invocation: true` on ALL workflow skills (init, discuss, research, plan, apply, unify, verify, bugsweep, milestone, pause, resume). Only `dan:status` should have `disable-model-invocation: false` so Claude can auto-check status.

    **Body (each skill):** Brief description of what the skill does, what agents it spawns, and a placeholder `<execution_flow>` section noting "Full workflow implementation in Phase {N}". Reference `bin/dan-tools.cjs` for state operations using absolute path pattern: `node "$HOME/.claude/dan/bin/dan-tools.cjs" --cwd $PROJECT_DIR`.

    **Skill-specific details:**
    - `dan:init` - Initialize .planning/ directory, run discuss phase. No agent spawning (runs in-session).
    - `dan:discuss` - Deep interview phase. Runs in-session (interactive).
    - `dan:research` - Spawn dan-researcher agents in parallel waves, then dan-synthesizer. Arg: topic or phase number.
    - `dan:plan` - Spawn dan-planner agent for phase. Arg: phase number.
    - `dan:apply` - Execute plan tasks. Default in-session, spawn dan-qualifier for E/Q. Arg: plan path or phase number.
    - `dan:unify` - Close loop, write SUMMARY.md. Runs in-session. Arg: plan path.
    - `dan:verify` - Spawn dan-verifier agent. Arg: phase number.
    - `dan:bugsweep` - Spawn dan-auditor agent in recursive loop. Arg: phase number.
    - `dan:milestone` - Chain research->plan->apply->unify->verify->bugsweep. Arg: milestone name.
    - `dan:status` - Show progress, suggest next action. Runs in-session.
    - `dan:pause` - Save session state for later. Runs in-session.
    - `dan:resume` - Restore session from STATE.md. Runs in-session.

    **Shared workflow skill** (`.claude/skills/dan-workflow/SKILL.md`):
    - `name: dan-workflow` (no colon -- this is a context skill, not user-invocable)
    - `description: Core DAN workflow protocol and conventions`
    - `disable-model-invocation: true`
    - Body contains:
      1. Loop protocol: PLAN -> APPLY -> UNIFY -> SUMMARY. No orphan plans. Every plan must close.
      2. E/Q protocol: Executor produces, qualifier independently re-reads and grades. Four statuses: PASS, PASS_WITH_CONCERNS, NEEDS_REVISION, FAIL. Qualifier never has Write tool access.
      3. Diagnostic routing: On failure, classify root cause as intent (wrong goal), spec (wrong plan), or code (wrong implementation) before applying fix.
      4. State protocol: Always use `dan-tools.cjs` for state reads/writes. Never parse STATE.md with inline regex in agents.
      5. Two-level rule: Skills orchestrate. Agents execute. Agents MUST NOT spawn other agents.
      6. Fresh context rule: Each agent gets a clean context window. State is passed via files, not agent memory.
  </action>
  <verify>
    <automated>node bin/dan-tools.cjs frontmatter parse .claude/skills/dan-plan/SKILL.md && node bin/dan-tools.cjs frontmatter parse .claude/skills/dan-workflow/SKILL.md</automated>
  </verify>
  <done>
    - 12 skill SKILL.md files exist under .claude/skills/dan-*/
    - Each has valid YAML frontmatter with name, description, argument-hint (where applicable), disable-model-invocation
    - All workflow skills have disable-model-invocation: true; dan:status has false
    - dan-workflow/SKILL.md contains loop protocol, E/Q protocol, diagnostic routing, two-level rule
  </done>
</task>

<task type="auto">
  <name>Task 2: Agent definitions with tool restrictions</name>
  <files>.claude/agents/dan-researcher.md, .claude/agents/dan-synthesizer.md, .claude/agents/dan-planner.md, .claude/agents/dan-executor.md, .claude/agents/dan-qualifier.md, .claude/agents/dan-verifier.md, .claude/agents/dan-auditor.md</files>
  <action>
    Create 7 agent definition files in `.claude/agents/dan-*.md`. Each agent needs:

    **Frontmatter (all agents):**
    - `name: dan-<role>`
    - `description:` one-line purpose
    - `tools:` allowlisted tools (critical for security/independence)
    - `skills:` reference `dan-workflow` for shared protocol injection
    - `model:` selection per role

    **Agent-specific definitions:**

    1. `dan-researcher.md`:
       - tools: Read, Grep, Glob, Bash, WebFetch
       - model: haiku (exploration is breadth, not depth)
       - Role: Single research pass on specific topic. Read codebase, search web, produce structured findings.
       - MUST NOT contain any instructions to spawn agents.

    2. `dan-synthesizer.md`:
       - tools: Read, Grep, Glob, Write
       - model: inherit (synthesis needs reasoning depth)
       - Role: Merge multiple research outputs, identify gaps, assess confidence, produce SUMMARY.md.
       - MUST NOT contain any instructions to spawn agents.

    3. `dan-planner.md`:
       - tools: Read, Grep, Glob, Write
       - model: inherit
       - Role: Generate executable plan from research + project context. Produce PLAN.md with frontmatter, tasks, verification.
       - MUST NOT contain any instructions to spawn agents.

    4. `dan-executor.md`:
       - tools: Read, Write, Edit, Bash, Glob, Grep
       - model: inherit
       - Role: Execute single plan task. Full tool access. Follows task action instructions precisely.
       - MUST NOT contain any instructions to spawn agents.

    5. `dan-qualifier.md`:
       - tools: Read, Grep, Glob, Bash
       - model: inherit
       - Role: Independent re-read of executor output. Grade against acceptance criteria. Return PASS/PASS_WITH_CONCERNS/NEEDS_REVISION/FAIL with reasoning. CRITICAL: No Write or Edit tools -- qualifier must not modify what it is reviewing.
       - MUST NOT contain any instructions to spawn agents.

    6. `dan-verifier.md`:
       - tools: Read, Grep, Glob, Bash
       - model: sonnet (verification is pattern-matching, not creative)
       - Role: Check deliverables against phase goals and acceptance criteria. Run tests. Write VERIFICATION.md results.
       - Note: Has Bash for running test commands but no Write/Edit for modifying source.
       - MUST NOT contain any instructions to spawn agents.

    7. `dan-auditor.md`:
       - tools: Read, Write, Edit, Bash, Glob, Grep
       - model: inherit
       - Role: Find issues, fix them, report. Full tool access because auditor needs to make fixes. Follows diagnostic routing (classify before fix).
       - MUST NOT contain any instructions to spawn agents.

    **Body structure for each agent:**
    Use `<role>` tags for role definition, include:
    - Purpose statement
    - Specific responsibilities
    - What this agent does NOT do (boundaries)
    - Reference to dan-tools.cjs for state operations: `node "$HOME/.claude/dan/bin/dan-tools.cjs" --cwd "$PROJECT_DIR"`
    - Placeholder for full execution flow: "Detailed execution flow will be implemented in Phase {N}"

    **CRITICAL VERIFICATION:** After writing each agent, confirm:
    - No mention of "spawn agent", "Task tool", "Agent tool", or "subagent" in the body
    - No instructions that would cause the agent to create or invoke other agents
    - This enforces FOUND-09 (two-level hierarchy)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const path=require('path');const dir='.claude/agents';const files=fs.readdirSync(dir).filter(f=>f.startsWith('dan-'));const results={total:files.length,agents:[],violations:[]};files.forEach(f=>{const content=fs.readFileSync(path.join(dir,f),'utf-8');results.agents.push(f);if(/spawn.*agent|Agent\s+tool|subagent/i.test(content))results.violations.push(f)});console.log(JSON.stringify(results,null,2))"</automated>
  </verify>
  <done>
    - 7 agent files exist under .claude/agents/dan-*.md
    - Each has valid frontmatter with name, description, tools, skills, model
    - dan-qualifier has NO Write or Edit in tools list
    - dan-verifier has NO Write or Edit in tools list
    - dan-researcher uses haiku model; dan-verifier uses sonnet model; others use inherit
    - All agents reference dan-workflow in skills: frontmatter
    - ZERO agents contain instructions to spawn other agents (two-level hierarchy enforced)
  </done>
</task>

</tasks>

<verification>
- All 12 skills discoverable: `ls .claude/skills/dan-*/SKILL.md | wc -l` returns 13 (12 entry points + 1 workflow)
- All 7 agents exist: `ls .claude/agents/dan-*.md | wc -l` returns 7
- Frontmatter valid on all files: `node bin/dan-tools.cjs frontmatter parse` succeeds on each
- No agent spawning instructions in any agent definition (grep for "spawn agent", "Agent tool", "subagent")
- dan-qualifier and dan-verifier have no Write/Edit in tools
- All agents reference dan-workflow skill
</verification>

<success_criteria>
- 12 skill entry points + 1 workflow skill = 13 SKILL.md files
- 7 agent definition files
- All frontmatter valid and parseable
- Two-level hierarchy enforced: zero agents reference agent spawning
- Tool restrictions correct per role (qualifier/verifier = read-only, executor/auditor = full access)
- dan-workflow skill contains loop protocol, E/Q protocol, diagnostic routing, state protocol, two-level rule
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
