---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - bin/lib/template.cjs
  - bin/lib/dependency.cjs
  - bin/lib/phase.cjs
  - bin/templates/plan.md
  - bin/templates/summary.md
  - bin/templates/state.md
  - bin/templates/project.md
  - bin/templates/config.json
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
  - FOUND-10

must_haves:
  truths:
    - "Running `node bin/dan-tools.cjs template fill plan --phase 01 --plan 01` produces a valid PLAN.md skeleton with frontmatter"
    - "Running `node bin/dan-tools.cjs dependency analyze .planning/phases/01-foundation/` returns topologically sorted plan execution order"
    - "Running `node bin/dan-tools.cjs state advance-plan` atomically updates STATE.md plan counter and progress bar"
    - "Dependency analysis detects circular dependencies and reports an error"
  artifacts:
    - path: "bin/lib/template.cjs"
      provides: "Template filling for plan, summary, state, project scaffolds"
      exports: ["handle"]
    - path: "bin/lib/dependency.cjs"
      provides: "Topological sort of plans by depends_on, wave assignment"
      exports: ["handle", "topologicalSort", "assignWaves"]
    - path: "bin/lib/phase.cjs"
      provides: "Phase directory operations: find, list, complete, advance"
      exports: ["handle"]
    - path: "bin/templates/plan.md"
      provides: "Plan template with frontmatter skeleton and XML task structure"
    - path: "bin/templates/summary.md"
      provides: "Summary template with sections for plan-vs-actual comparison"
  key_links:
    - from: "bin/dan-tools.cjs"
      to: "bin/lib/template.cjs"
      via: "require and switch dispatch"
      pattern: "case 'template'"
    - from: "bin/dan-tools.cjs"
      to: "bin/lib/dependency.cjs"
      via: "require and switch dispatch"
      pattern: "case 'dependency'"
    - from: "bin/dan-tools.cjs"
      to: "bin/lib/phase.cjs"
      via: "require and switch dispatch"
      pattern: "case 'phase'"
    - from: "bin/lib/dependency.cjs"
      to: "bin/lib/frontmatter.cjs"
      via: "require for parsing plan frontmatter depends_on"
      pattern: "frontmatter\\.parse"
    - from: "bin/lib/phase.cjs"
      to: "bin/lib/state.cjs"
      via: "require for state updates on phase advance"
      pattern: "state.*set|advance"
---

<objective>
Build template filling, dependency analysis, phase operations, and progress tracking. These are the remaining CLI tool capabilities that skills and agents need for plan generation and execution ordering.

Purpose: Templates provide consistent scaffolding for plans and summaries. Dependency analysis enables wave-based parallel execution (FOUND-04). Phase operations with progress tracking (FOUND-10) keep STATE.md current as work progresses.
Output: Three new lib modules (template, dependency, phase) with templates directory and full test coverage.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<!-- Depends on Plan 01 outputs -->
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan needs -->

From bin/lib/core.cjs (created by Plan 01):
```javascript
module.exports = { output, error, atomicWriteFileSync, execGit, toPosixPath, loadConfig };
// output(result, raw, rawValue) - JSON to stdout
// error(msg) - stderr + exit(1)
// atomicWriteFileSync(filePath, content) - temp+rename
// loadConfig(cwd) - returns merged config with defaults
```

From bin/lib/frontmatter.cjs (created by Plan 01):
```javascript
module.exports = { handle, parse, serialize };
// parse(filePath) - returns {frontmatter: Object, body: String}
// serialize(obj, body) - returns "---\nyaml\n---\nbody"
```

From bin/lib/state.cjs (created by Plan 01):
```javascript
module.exports = { handle };
// handle(cwd, args, raw) - dispatches read|get|set|patch
// args: ['set', 'fieldName', 'value'] for field updates
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Template filling and dependency analysis modules</name>
  <files>bin/lib/template.cjs, bin/lib/dependency.cjs, bin/templates/plan.md, bin/templates/summary.md, bin/templates/state.md, bin/templates/project.md, bin/templates/config.json</files>
  <behavior>
    - template fill plan --phase 01 --plan 02 produces PLAN.md with frontmatter (phase, plan, type, wave, depends_on, files_modified, autonomous, must_haves) and XML task skeleton
    - template fill summary --phase 01 --plan 02 produces SUMMARY.md with objective, what-was-built, plan-vs-actual sections
    - template fill state produces STATE.md initial scaffold
    - template fill project produces PROJECT.md initial scaffold
    - dependency analyze reads all PLAN.md files in a phase dir, parses depends_on from frontmatter, returns topologically sorted plan IDs
    - dependency analyze detects circular deps and returns error JSON
    - dependency waves returns plans grouped by execution wave number
    - topologicalSort([{id, depends_on}]) is exported for direct use
  </behavior>
  <action>
    Create template files in `bin/templates/`:
    - `plan.md`: Frontmatter skeleton with all required fields (phase, plan, type, wave, depends_on, files_modified, autonomous, requirements, must_haves). Body with objective, context, tasks (XML skeleton with one auto task), verification, success_criteria, output sections. Use `{{PHASE}}`, `{{PLAN}}`, `{{PHASE_NAME}}` placeholders.
    - `summary.md`: Frontmatter (phase, plan, completed date). Body with objective, what-was-built, plan-vs-actual, decisions, deferred sections.
    - `state.md`: Full STATE.md schema from research (project reference, current position, performance metrics, accumulated context, session continuity).
    - `project.md`: PROJECT.md scaffold with project name, core value, requirements, constraints, key decisions sections.
    - `config.json`: Default dan.config.json content.

    Create `bin/lib/template.cjs`:
    - `handle(cwd, args, raw)`: dispatch fill subcommand
    - `fill type --phase N --plan M [--name phaseName]`: read template from bin/templates/, replace placeholders, output result
    - Placeholders: `{{PHASE}}`, `{{PLAN}}`, `{{PHASE_NAME}}`, `{{DATE}}`, `{{PADDED_PHASE}}`, `{{PADDED_PLAN}}`
    - Find templates relative to __dirname (so works regardless of cwd)

    Create `bin/lib/dependency.cjs`:
    - `handle(cwd, args, raw)`: dispatch analyze|waves subcommands
    - `analyze phaseDirPath`: glob for *-PLAN.md files, parse frontmatter to get id and depends_on, run topologicalSort, return ordered plan IDs
    - `waves phaseDirPath`: same as analyze but group into waves (wave 1 = no deps, wave N = max(dep waves) + 1)
    - `topologicalSort(plans)`: Kahn's algorithm or DFS with cycle detection. Input: [{id, depends_on: []}]. Output: ordered IDs. Throws on circular dependency.
    - `assignWaves(plans)`: compute wave number for each plan. Returns [{id, wave}].
    - Export topologicalSort and assignWaves for direct use by other modules.

    Register both modules in dan-tools.cjs router: "template" -> lib/template.cjs, "dependency" -> lib/dependency.cjs.
  </action>
  <verify>
    <automated>node --test bin/tests/test-template.cjs && node --test bin/tests/test-dependency.cjs</automated>
  </verify>
  <done>
    - `node bin/dan-tools.cjs template fill plan --phase 01 --plan 03` outputs valid PLAN.md with correct phase/plan in frontmatter
    - `node bin/dan-tools.cjs dependency analyze .planning/phases/01-foundation/` returns ordered plan IDs
    - Circular dependency input produces error JSON, not a hang or crash
    - All 5 template files exist in bin/templates/
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase operations and progress tracking</name>
  <files>bin/lib/phase.cjs</files>
  <behavior>
    - phase find 01 returns phase directory path (.planning/phases/01-foundation/)
    - phase find 1 also works (auto-pads to 01)
    - phase list returns all phase directories with status
    - phase advance-plan updates STATE.md: increments plan counter, updates progress bar, updates last_activity
    - phase complete updates STATE.md: marks phase complete, advances to next phase
    - Progress bar renders correctly: [=====.....] 50%
  </behavior>
  <action>
    Create `bin/lib/phase.cjs`:
    - `handle(cwd, args, raw)`: dispatch find|list|advance-plan|complete subcommands
    - `find phaseNumber`: normalize number (pad to 2 digits), glob .planning/phases/ for matching directory, return path. Handle decimal phases (2.1) and missing phases gracefully.
    - `list`: scan .planning/phases/ directories, parse STATE.md for current phase, return [{phase, name, status, plans_complete, plans_total}]
    - `advance-plan`: read STATE.md, increment "Plan" field (X of Y), recalculate progress percentage, update progress bar string, set last_activity to current date + "Completed plan {phase}-{plan}". All writes via atomicWriteFileSync through state module.
    - `complete`: mark current phase complete in STATE.md, advance Phase field to next. Update progress bar. Set status to "Phase complete".
    - Progress bar: generate `[====.....]` string where = is proportional to percentage, total width 10 chars.

    Register in dan-tools.cjs router: "phase" -> lib/phase.cjs.
  </action>
  <verify>
    <automated>node --test bin/tests/test-phase.cjs</automated>
  </verify>
  <done>
    - `node bin/dan-tools.cjs phase find 01` returns .planning/phases/01-foundation/ path
    - `node bin/dan-tools.cjs phase find 1` also works (auto-pad)
    - `node bin/dan-tools.cjs phase list` returns array of phase objects
    - advance-plan updates STATE.md Plan field and progress bar atomically
    - Progress bar renders correctly for 0%, 50%, 100%
  </done>
</task>

</tasks>

<verification>
- Template fill produces valid markdown with correct frontmatter for all template types
- Dependency analysis correctly orders plans: independent plans first, dependent plans after their dependencies
- Circular dependency detection works (returns error, does not hang)
- Phase find handles padding (1 -> 01) and returns correct paths
- Progress tracking updates STATE.md atomically with correct plan count and progress bar
- All tests pass: `node --test bin/tests/`
</verification>

<success_criteria>
- `node bin/dan-tools.cjs template fill plan --phase 02 --plan 01` produces valid PLAN.md
- `node bin/dan-tools.cjs dependency analyze .planning/phases/01-foundation/` returns correct order
- `node bin/dan-tools.cjs phase find 01` returns correct directory path
- `node bin/dan-tools.cjs phase advance-plan` updates STATE.md correctly
- Test suite green: `node --test bin/tests/`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
